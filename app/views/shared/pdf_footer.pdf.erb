<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">

    <script>
      function number_pages() {
        var vars = {};
        var query = document.location.search.substring(1).split("&");

        for (var i = 0; i < query.length; i++) {
          var pair = query[i].split("=", 2);

          vars[pair[0]] = decodeURIComponent(pair[1] || "");
        }

        var fields = ["page", "topage"];

        fields.forEach(function(field) {
          var elements = document.getElementsByClassName(field);

          for (var i = 0; i < elements.length; i++) {
            elements[i].textContent = vars[field];
          }
        });
      }
    </script>
  </head>

  <body onload="number_pages()">
    <div
      style="
        width:100%;
        text-align:center;
        font-size:10px;
        color:#666;
      "
    >
      <%= t("assignments.export.page", page: '<span class="page"></span>', total: '<span class="topage"></span>').html_safe %>
    </div>
  </body>
</html>
