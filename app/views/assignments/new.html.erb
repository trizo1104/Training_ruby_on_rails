<% @page_title = t("assignments.new.page_title") %>
<% @active_nav = "create" %>

<div class="space-y-6">
  <%= render "shared/page_header", title: t("assignments.new.title"), description: t("assignments.new.description"), action_label: t("common.actions.back"), action_path: assignments_path %>

  <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", assignment: @assignment %>
  </div>
</div>
