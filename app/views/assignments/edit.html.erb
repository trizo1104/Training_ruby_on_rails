<% @page_title = t("assignments.edit.page_title") %>
<% @active_nav = "create" %>

<div class="space-y-6">
  <%= render "shared/page_header", title: t("assignments.edit.title"), description: t("assignments.edit.description"), action_label: t("common.actions.view_details"), action_path: assignment_path(@assignment) %>

  <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", assignment: @assignment %>
  </div>
</div>
