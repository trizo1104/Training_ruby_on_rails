<% @page_title = t("users.new.page_title") %>
<% @active_nav = "users" %>

<div class="space-y-6">
  <%= render "shared/page_header",
             title: t("users.new.title"),
             description: t("users.new.description") %>

  <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form" %>
  </div>
</div>